#Add Road Symbol Fields

##Parameters
**Input layer** - The road layer to add the road symbols to to.

##Description
Adds columns that can store information used to display road symbols as labels
on a map. Also creates a view in the database (if applicable) for a road
symbols layer.
